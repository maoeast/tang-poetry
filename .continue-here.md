# Current State

- 任务：诗歌分类浏览页 `/browse` 已完成开发
- 已完成：`/browse` 路由（服务端组件，按 tags 字段分 7 体裁 + 未分类，366 首全覆盖）
- 已完成：`lib/images/repository.ts` 新增 `getAllPoetryImages()` + `getPlaceholderImage()`
- 已完成：首页 `app/page.tsx` 新增"诗歌分类"功能卡片
- 已完成：`lib/review/scheduler.ts` 预存类型错误修复（缺繁简字段 + 缺第三参数）
- 已完成：`tsconfig.json` 排除 `scripts/` 解决脚本预存类型错误
- 产出物：`app/browse/page.tsx`、`components/browse/poetry-card.tsx`、`components/browse/category-section.tsx`、`lib/browse/repository.ts`、`tests/browse/repository.test.ts`、`tests/images/repository-batch.test.ts`

# Next Action

- 启动 `npm run dev`，浏览器打开 `/browse` 验证视觉布局与点击跳转

# Blockers

- None

# Key Decisions

- 浏览页单次 `poetry.findMany` + JS 按 `FORM_TAGS` 分组，不走多次 `array_contains` 查询：366 行无需优化
- 图片批量走 `getAllPoetryImages()` 返回 `Map<string, PoetryImage>`，避免 366 次单独查询
- `tsconfig.json` 排除 `scripts/`：脚本用 `import.meta.main` 等 Node 特有 API，不属于 Next.js 构建范围

# Relevant Files

- `app/browse/page.tsx`
- `components/browse/poetry-card.tsx`
- `components/browse/category-section.tsx`
- `lib/browse/repository.ts`
- `lib/images/repository.ts`

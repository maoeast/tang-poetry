# Current State

- 任务：新增诗人（Author）页面，包含诗人头像、简介、作品列表
- 已完成：环境修复 — Prisma 迁移同步（3 个 pending migration 已应用）、`npm run import:ts300` 重新导入补全 `sourceUid` + 简繁字段、`npm run import:image-assets` 导入 366 条 ImageAsset
- 已完成：IDM 拦截音频下载 bug 修复 — 两个播放器组件改为 `fetch()` + Blob URL 方式加载音频
- 产出物：`components/poetry/immersive-poetry-stage.tsx`、`components/review/review-poetry-stage.tsx` 已修改

# Next Action

- 执行 `superpowers:brainstorming` 技能，规划诗人页面 `/author/[authorName]` 的数据模型、页面结构和组件设计

# Blockers

- None

# Key Decisions

- 音频播放统一用 `fetch() → Blob → URL.createObjectURL()` 加载，避免 IDM 等下载管理器扩展拦截 `.mp3` 请求
- `sourceUid` 字段存储源数据 UUID（`ts300.simple.json` 的 `id`），用于映射音频文件名
- 诗人页面设计参考古文岛 `guwendao.net/authorv_*.aspx`：头像 + 简介 + 作品列表

# Relevant Files

- `components/poetry/immersive-poetry-stage.tsx`
- `components/review/review-poetry-stage.tsx`
- `prisma/schema.prisma`（需要新增 Author 模型或扩展 Poetry 查询）
- `lib/poetry/repository.ts`
- `lib/audio.ts`
- `data/ts300.simple.json`（源数据，诗人姓名在 author 字段）

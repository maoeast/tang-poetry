# Current State
- 任务：诗歌分类页 `/browse` 新中式古风设计改造
- 已完成：引首章印章标题 + 古风大写数字计数 + 卡片居中留白 + Hero 去框化 + CSS sticky 吸顶导航
- 产出物：
  - `components/browse/sticky-category-nav.tsx` — CSS sticky 吸顶分类导航（Client Component，仅处理 scrollIntoView）
  - `components/browse/category-section.tsx` — 引首章印章 + 衬线体标题 + 大写数字计数
  - `components/browse/poetry-card.tsx` — 居中文字 + 留白 + 轻边框
  - `app/browse/page.tsx` — 去框化 Hero + full-bleed sticky nav + 分类列表

# Next Action
- 在浏览器中验证 `/browse` 页面的视觉效果和交互行为（吸顶导航、锚点跳转、卡片 hover）

# Blockers
- None

# Key Decisions
- Sticky nav 用纯 CSS `sticky top-0`，不用 JS `fixed` + scroll listener：更简洁，自然位置可见，滚动时自动吸附
- Hero 去框化：移除白色卡片包裹，直接在宣纸底色渲染，用 `w-24 h-px` 淡墨线做过渡

# Relevant Files
- `app/browse/page.tsx`
- `components/browse/sticky-category-nav.tsx`
- `components/browse/category-section.tsx`
- `components/browse/poetry-card.tsx`
- `app/globals.css`

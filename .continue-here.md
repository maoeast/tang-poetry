# Current State
- 任务：诗歌详情页 `/poetry/[id]` 排版与控件重构已完成；下一步为 `/browse` 页面添加搜索功能
- 已完成：
  - 诗句从两行拆分为四行（`splitCoupletLines`），拼音/音频 timing 按 originalIndex 映射
  - 字距收紧 `.cjk-grid-char` 2em→1.35em，标点 `.poetry-punct` margin-inline 收紧
  - 未激活行对比度 text-ink-400 opacity-70 → text-ink-600
  - 拼音/简繁开关移至底部控制栏，顶部仅保留动态返回按钮 (`BackButton` / `router.back()`)
  - 底部栏改为浅灰单行：播放按钮 + 带圆点进度条 + 时间 + [简][拼] 切换
  - 标题/作者/正文居中对齐
- 产出物：
  - `components/lyrics/lyrics-window.tsx` — originalIndex + originalLineCount + text-center
  - `components/poetry/immersive-poetry-stage.tsx` — splitCoupletLines + 紧凑控制栏 + formatTime
  - `components/poetry/back-button.tsx` — router.back() 动态返回
  - `components/poetry/script-variant-toggle.tsx` — compact + dark 模式，标签缩写（简/繁）
  - `app/globals.css` — .cjk-grid-char 1.35em + .poetry-punct + pinyin letter-spacing

# Next Action
- 为 `/browse` 页面添加搜索功能：搜索框 UI + 按标题/作者/正文过滤诗歌列表

# Blockers
- None

# Key Decisions
- 四行排版：每个逗号类标点处拆分，拼音按 CJK 字数分配到各行，音频 timing 通过 originalIndex 映射回联句级别
- 底部控制栏去掉倍速控制，只保留显示模式开关（简/拼），进度条加可拖拽圆点

# Relevant Files
- `app/browse/page.tsx`
- `components/browse/sticky-category-nav.tsx`
- `components/browse/category-section.tsx`
- `components/browse/poetry-card.tsx`
- `lib/browse/repository.ts`
- `lib/poetry/repository.ts`

# Current State
- 任务：诗歌详情页 `/poetry/[id]` 视觉重构 — 左画右诗 + 播放器对齐 + 渐隐滚动 + 右栏居中
- 已完成：
  - 双栏 flex 布局（左图高度锚点 + 右栏 maxHeight 同步 + 播放器沉底 + 条件 fade mask）
  - 右栏内容水平居中（lg:max-w-xl + lg:mx-auto）
  - 工作区全部未提交文件已提交（头像/脚本/首页/文档/截图）
- 产出物：
  - `components/poetry/immersive-poetry-stage.tsx`
  - `app/globals.css`（`.scrollbar-hide` + `.poetry-fade-mask`）
- 已提交：
  - `0bf181f` — 右栏居中
  - `e783dc9` — 头像+脚本+首页+文档

# Next Action
- 诗歌详情页视觉任务已全部完成，等待新任务

# Blockers
- None

# Key Decisions
- flex + items-start 替代 grid stretch：避免 grid 行高被右列内容撑开的循环依赖
- JS maxHeight 同步：posterRef 自然高度 → rightColRef.style.maxHeight
- 条件性 fade mask：ResizeObserver 检测 scrollHeight > clientHeight，短诗不裁剪
- 右栏居中：lg:max-w-xl + lg:mx-auto，移动端保持全宽

# Relevant Files
- `components/poetry/immersive-poetry-stage.tsx`
- `components/lyrics/lyrics-window.tsx`
- `app/globals.css`

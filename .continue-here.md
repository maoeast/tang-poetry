# Current State
- 任务：Task B 批量预生成 AI 讲解文本 + 讲解音频
- 已完成：
  - Phase 1：`scripts/generate-ai-explanations.ts` 生成 650 条讲解文本写入 DB（325 poems × 2 audiences）
  - Phase 2：`scripts/generate-explain-audio.ts` 生成 650 个 MP3（并发 50，566MB）
  - 两者均零失败
- 产出物：
  - `scripts/generate-ai-explanations.ts` — 批量讲解文本脚本
  - `scripts/generate-explain-audio.ts` — 批量讲解音频脚本（支持 --concurrency）
  - `public/audio/explain/{poetryId}_{audience}.mp3` — 650 个音频文件
  - `Poetry.aiExplanation` JSONB — 650 条缓存记录

# Next Action
- Phase 3：修改 `components/poetry/ai-explanation-card.tsx`，让讲解文本从 DB 缓存直接展示（无需点击加载按钮），并添加音频播放器（检测 `public/audio/explain/{poetryId}_{audience}.mp3`）
- 第一步：在 `lib/audio.ts` 添加 `getExplainAudioUrl(poetryId, audience)` 函数

# Blockers
- None

# Key Decisions
- child_v1 音色 yuanqishaonv（元气少女），general_v1 音色 linjiajiejie（邻家姐姐）
- 音频路径 `public/audio/explain/{poetryId}_{audience}.mp3`，已加入 .gitignore
- 保留现有 `/api/ai/explain` 实时调用流程作为回退（无缓存时仍可实时生成）

# Relevant Files
- `components/poetry/ai-explanation-card.tsx` — Phase 3 改造目标
- `lib/audio.ts` — 需扩展 getExplainAudioUrl()
- `app/poetry/[id]/page.tsx` — 服务端传递讲解数据给客户端组件（Phase 4）
- `lib/ai/prompts.ts` — getExplanationCacheKey() 复用
- `lib/ai/deepseek.ts` — PoetryExplanation 类型复用
- `docs/task-batch-ai-explain-tts.md` — 完整实现计划

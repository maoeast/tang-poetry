# Current State

- 最新目标：实施已批准的 Tauri 离线优先桌面端和同步计划。
- 已完成：
  - 代码和 UI 稳定化计划 `docs/superpowers/plans/2026-07-11-code-ui-stabilization.md` 已完成实施并勾选完成。
  - 构建阻断、图片生成测试版本漂移、TTS 脚本 lazy import、首页/浏览页/我的页移动端布局和浏览页图片 priority 已修复。
  - 验证已跑：`npm run lint`（0 errors，仍有既有 warnings）、`npm test`（286 pass）、`npm run build`、Playwright 桌面/375px 截图检查。
  - 用户明确批准“断网也能完整学习、写记录、复习、收藏”的 Tauri 离线优先方向。
  - 新计划已写入 `docs/superpowers/plans/2026-07-11-tauri-offline-desktop-sync.md`。
  - `docs/INDEX.md` 已加入 Tauri 离线优先桌面端和同步计划路由。
  - `PROJECT_CONTEXT.md` 已记录已批准的桌面方向和 SQLite 例外边界。
- 未实施：
  - Tauri 离线优先计划的 9 个任务尚未开始改代码。

# Next Action

- 实施 `docs/superpowers/plans/2026-07-11-tauri-offline-desktop-sync.md` 的任务 1：先新增 `lib/sync/events.ts` 和 `lib/sync/projections.ts`，定义 SyncEvent 类型、事件 payload 类型和最小事件投影纯函数测试，然后运行 `npm test`。

# Blockers

- None

# Key Decisions

- 不使用 Electron。
- 不把当前 Next.js App Router 服务端应用直接塞进 Tauri sidecar。
- 不把 Web 版改成静态导出应用。
- Web 版继续使用 Next.js + Prisma + PostgreSQL。
- 桌面端使用 Tauri v2 + 本地 SQLite，断网时本机 SQLite 是离线真源。
- 新增 SQLite 本地数据层是用户明确批准的架构例外，覆盖 `AGENTS.md` 默认“不新增数据库引擎，除非用户要求”的限制。
- 同步采用 append-only `sync_event` 事件日志；`ReviewState` 不作为同步真源，只作为事件重放投影。
- Phase 1 仍不引入 `User` model，不做多账号系统，继续使用 `SYSTEM_USER_ID`。
- AI 讲解不承诺完全离线生成；离线只读已缓存讲解，未缓存内容联网后生成和同步。
- 内容快照和用户事件分开同步；图片运行时语义仍以 `ImageAsset` 为权威。

# Relevant Files

- `AGENTS.md`
- `.continue-here.md`
- `HANDOFF.md`
- `PROJECT_CONTEXT.md`
- `docs/INDEX.md`
- `docs/superpowers/plans/2026-07-11-tauri-offline-desktop-sync.md`
- `docs/superpowers/plans/2026-07-11-code-ui-stabilization.md`
- `prisma/schema.prisma`
- `lib/review/scheduler.ts`
- `lib/challenge/engine.ts`
- `lib/favorite/repository.ts`
- `package.json`

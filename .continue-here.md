# Current State
- 任务：收藏功能 + 诗人头像增强 + 清理冗余页面标签 — **已完成并推送**
- 已完成：
  - `lib/favorite/repository.ts` — `isFavoritePoem` / `toggleFavoritePoem` / `getFavoritePoems`，DI 模式，返回 `BrowsePoem[]`
  - `components/poetry/favorite-toggle.tsx` — `useTransition` + 乐观更新 + 心形 SVG
  - 详情页顶栏接入收藏按钮（`app/poetry/[id]/page.tsx` + `poetry-detail.tsx`）
  - 我的页面"我的收藏"区块，复用 `<PoetryCard>` 网格 + 空状态（`app/me/page.tsx` + `profile-summary.tsx`）
  - `tests/favorite/repository.test.ts` — 8 个测试全过
  - 首页今日诗词作者头像改为真实头像（`today-poetry-hero.tsx` 传入 `authorAvatarUrl`）
  - 我的页面"诗人缘分" `#1/#2/#3` 改为真实诗人头像（`getAuthorAvatarUrl` helper）
  - 移除 4 页右上角冗余标签（诗人列表/诗歌浏览/温故知新/我的）
- 产出物：`729f30f`（12 files），已推送 origin/main

# Next Action（原子级）
- 无待办。本任务收尾，可接手其它工作

# Blockers
- None

# Key Decisions（影响后续实现）
- `getFavoritePoems` 返回 `BrowsePoem[]` 而非裸 `poetryId[]`：me 页直接喂给 `<PoetryCard>`，避免重复映射
- 收藏按钮放详情页顶栏（紧挨"进入挑战"），不用 poster overlay — `PoetryPoster` 组件尚未接入
- `getAuthorAvatarUrl(name)` 为同步函数，直接从 `data/authors.json` 查找，无需异步

# Relevant Files（最小集）
- `lib/favorite/repository.ts` — 收藏 lib 核心
- `components/poetry/favorite-toggle.tsx` — 收藏按钮组件
- `lib/author/repository.ts` — `getAuthorAvatarUrl` 在 line 121

# 可能的下一步方向（未启动，待用户确认）
- 详情页 `ImmersivePoetryStage` 和复习页 `ReviewPoetryStage` 的 `PoetryTitleAuthor` 也缺 `authorAvatarUrl`（同首页问题，用户未提）
- 分享海报组件 `components/poster/` 已建但未接入任何页面
- 全局诗歌搜索（860 首诗无搜索入口）

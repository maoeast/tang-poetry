# Current State
- 任务：检查 ts300/gs300/sc200 共 860 首诗的 `Poetry.aiExplanation` 覆盖率；若不完整且有历史记录可恢复则恢复，否则用 DeepSeek API 重新批量生成
- 已完成（上轮）：translation/annotation 已 100% 覆盖（860/860）；新增 `data/poetry-annotations.json` 备份；新增 `scripts/{dump,restore}-annotations.ts`
- 已完成（本轮）：修正 sc200-0202 错误分类标签（移除 `乐府` + `儿童`）
- 产出物：`scripts/generate-ai-explanations.ts`（已存在的批量生成脚本，支持 `--write / --id / --rate-limit`）；`scripts/clear-bad-ts300-ai-cache.ts`（曾经的清理脚本，可查历史清除原因）

# Next Action（原子级）
- 跑 `npx tsx scripts/generate-ai-explanations.ts`（默认 dry-run）输出全库 aiExplanation 覆盖率，列出缺失 poem ids；同时 `git show 06b8ae9 -- scripts/clear-bad-ts300-ai-cache.ts` 看上次清缓存时哪些 id 被清、原因是什么，判断是恢复还是重生成

# Blockers
- None

# Key Decisions（影响后续实现）
- aiExplanation schema：`{ [audience_promptVersion]: { emotion, imagery, summary, cachedAt } }`，audience 只允许 `child_v1` / `general_v1`，promptVersion 一期固定 `v1`（见 CLAUDE.md 与 `lib/ai/prompts.ts`）
- 现成批量工具 `scripts/generate-ai-explanations.ts` 已含 DeepSeek 调用 + 重试 + 断点续跑，不要另写脚本
- 完成后必须跑 `npm run dump:annotations` 把新生成的 aiExplanation 同步进 `data/poetry-annotations.json` 入 git

# Relevant Files（最小集）
- `scripts/generate-ai-explanations.ts` — 批量生成入口
- `scripts/clear-bad-ts300-ai-cache.ts` — 历史清除记录，查上次为什么清
- `lib/ai/deepseek.ts` — DeepSeek 调用与 prompt 拼装
- `lib/ai/prompts.ts` — audience/cache key 规则
- `scripts/dump-annotations.ts` — 生成完毕后跑一次入库备份
- `data/poetry-annotations.json` — 当前备份（含 translation/annotation/aiExplanation）

# 历史背景（不要忽略）
- commit `74917d5` 曾记录 "650 条讲解文本 + 650 个讲解音频全部生成"
- commit `06b8ae9` "清理错误 AI 缓存与解释音频残留" 把大部分清掉了，目前 DB 只剩 2 条（ts300-0002 等的 child_v1）
- 当时无 JSON 备份机制；本会话的 poetry-annotations.json 也是从当前（被清后的）DB dump 出来，所以里面只有 2 条 aiExplanation
- 推断：基本只能从零重新生成，但仍建议先看 `clear-bad-ts300-ai-cache.ts` 的 reason 列表，避免重蹈同样的 prompt 错误

# 工作树未提交文件（与本任务无关，跨会话遗留）
- `app/authors/page.tsx`、`components/**`、`lib/**`、`eslint.config.mjs`、`next-env.d.ts`、`package-lock.json`、若干 `scripts/{generate-*,submit-*,scrape-*}` 等都是上一次 wip 提交 `bc377e3` 后的零散改动，下一会话如果接手 AI 任务请不要顺手提交这些

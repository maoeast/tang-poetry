# Current State

- 已完成：366 首唐诗配图全部通过 apimart (gpt-image-2) 生成、下载、审核并导入数据库
- 已完成：图片三阶段流水线收口（prepare → apimart 批量提交 → download → finalize → import）
- 已完成：`AIimages/generated/` 366 张 PNG 拷贝到 `public/images/generated/` 并全部翻转为 ready
- 已完成：`ImageAsset` 表 366 条记录 upsert，style=`storybook-watercolor`，promptVersion=`v1`
- 产出物：`scripts/prepare-image-generation.ts`、`scripts/finalize-image-assets.ts`、`scripts/retry-image-downloads.ts`、`AIimages/submit-all.py`、`AIimages/download-all.py`、`AIimages/run-batch.py`、`data/image-assets.json`、`docs/image-generation-plan.md`、`docs/poetry-image-prompt-guide.md`、`docs/image-generation-manifest.md`

# Next Action

- 非关键卡片位（挑战页、相关推荐等）的运行时图片统一接入
- 生产部署配置与部署文档

# Blockers

- 无。图片批量生成任务已完整收口。

# Key Decisions

- 使用 apimart gpt-image-2 模型，2:3 竖版 2K 分辨率
- 批量生成策略：先预览 3 首确认质量 → fire-and-forget 提交全部 211 首 → 离线等待生成 → 批量下载
- 并行下载使用 3 worker，TLS 抖动时逐首重试
- 最终成品图放在 `public/images/generated/`，通过 `ImageAsset` 表驱动运行时展示

# Relevant Files

- `scripts/prepare-image-generation.ts`
- `scripts/finalize-image-assets.ts`
- `scripts/retry-image-downloads.ts`
- `data/image-assets.json`
- `public/images/generated/` (366 PNGs)
- `docs/poetry-image-prompt-guide.md`

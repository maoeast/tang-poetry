# Current State
- 任务：首页六宫格 + 分类浏览页来源过滤 + 古诗朝代精确划分
- 已完成：
  - `app/page.tsx`：4 卡片 → 6 卡片（古诗三百/唐诗三百/宋词精选/场景时令/挑战闯关/复习成长），六宫格布局（移动 2 列/桌面 3 列）
  - `app/browse/page.tsx`：支持 `?source=gs300|ts300|sc200`，标题动态显示
  - `lib/browse/repository.ts`：新增 SourceType、8 组 DYNASTY_GROUPS、classifyDynasty()，getPoetryByCategories 支持第 4 参数 source 过滤，gs300 按朝代分组（先秦/两汉/魏晋/南北朝/宋/元/明/清），sc200/ts300 按体裁分组（跳过空分类）
  - `components/browse/category-section.tsx`：SEAL_ABBR 新增朝代组印章缩写
  - `scripts/import-gs300.ts`：用 150+ 作者→朝代精确映射替换粗略索引范围分类
  - `data/gs300.simple.json`：278→277（移除孔绍安《侍宴咏石榴》至唐诗三百）
  - `data/ts300.simple.json` / `data/ts300.raw.json`：358→359（新增该诗）
  - DB 已重新导入：gs300 277 首（零"其他"）、ts300 359 首、daily seeds 已重建
  - 20 个浏览测试全通过（15 既有 + 5 新增 source 过滤测试），tsc --noEmit 无错误
- 产出物：6 个修改文件 + 1 个组件修改 + 3 个 JSON 数据文件更新 + 1 个导入脚本重写

# Next Action
- 修复唐诗三百配套图片乱套 bug：读取 `lib/images/repository.ts` 和 `data/image-assets.json`，定位 ImageAsset 的 poetryId 与 ts300 诗歌 ID 的对应关系是否因新增诗歌而错位
- 第一步：`git diff data/image-assets.json | head -100` 查看图片资源的变化模式

# Blockers
- 唐诗三百图片乱套：可能是 ts300 从 358→366（或更多）导致 poetryId 编号与 ImageAsset 不匹配

# Key Decisions
- getPoetryByCategories 的 source 放在最后（第 4 可选参数），保持已有测试零改动
- 孔绍安（初唐）从古诗三百移至唐诗三百，gs300 现在 277 首全部有精确朝代
- gs300 朝代映射基于作者而非索引位置，避免粗分错误

# Relevant Files
- `lib/images/repository.ts` — 图片查询逻辑
- `data/image-assets.json` — 图片资源元数据（poetryId 对应关系）
- `scripts/prepare-image-generation.ts` — 图片 prompt 生成脚本
- `scripts/finalize-image-assets.ts` — 扫描图片标记 ready
- `lib/browse/repository.ts` — source 过滤 + 朝代/体裁分类

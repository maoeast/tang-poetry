# Current State
- 任务：Task B Phase 3 前端讲解卡片改造 — 已完成
- 已完成：
  - `lib/audio.ts` 新增 `getExplainAudioUrl()` + `hasExplainAudioFile()`
  - `lib/poetry/repository.ts` 扩展 `PoetryDetail` 含 `explanations` + `explainAudio`，新增 `toExplanationCache()`
  - `components/poetry/ai-explanation-card.tsx` 全面改造：缓存优先展示 + 内联音频播放器 + API 回退
  - `components/poetry/poetry-detail.tsx` 传递 explanation 数据给卡片
  - `tests/poetry/repository.test.ts` 补齐新字段断言（211 pass）
- 产出物：前端讲解卡片可直接展示 DB 缓存讲解文本 + 播放预生成音频

# Next Action
- 回到主任务列表继续执行 Task 13（end-to-end integration testing），参照 `docs/superpowers/plans/2026-05-29-tang-poetry-app-phase-1.md`

# Blockers
- None

# Key Decisions
- 缓存优先：服务端从 DB 读取 aiExplanation，客户端直接展示，无缓存才走 `/api/ai/explain`
- `hasExplainAudioFile` 注入到 `PoetryRepositoryDependencies`，避免测试依赖真实文件系统
- 音频播放器仅在 `audioInfo.exists && explanation` 时展示

# Relevant Files
- `components/poetry/ai-explanation-card.tsx`
- `lib/poetry/repository.ts`
- `docs/superpowers/plans/2026-05-29-tang-poetry-app-phase-1.md`

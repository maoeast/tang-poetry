# Current State
- 任务：诗歌详情页 `/poetry/[id]` 视觉重构 — 左画右诗 + 播放器对齐 + 渐隐滚动
- 已完成：双栏 flex 布局（左图高度锚点 + 右栏 maxHeight 同步 + 播放器沉底 + 条件 fade mask）
- 产出物：
  - `components/poetry/immersive-poetry-stage.tsx`
  - `app/globals.css`（新增 `.scrollbar-hide` + `.poetry-fade-mask`）
- 已提交：本轮 3 文件已 commit

# Next Action
- 修改 `components/poetry/immersive-poetry-stage.tsx` 右栏内容区水平居中 — 当前标题/作者/正文/播放器全部靠左，右侧大片空白；需要让右栏 flex-col 内容整体水平居中（max-width + mx-auto 或 text-center 扩展到容器级别）

# Blockers
- None

# Key Decisions
- flex + items-start 替代 grid stretch：避免 grid 行高被右列内容撑开的循环依赖
- JS maxHeight 同步：posterRef 自然高度 → rightColRef.style.maxHeight
- 条件性 fade mask：ResizeObserver 检测 scrollHeight > clientHeight，短诗不裁剪

# Relevant Files
- `components/poetry/immersive-poetry-stage.tsx` — 右栏水平居中改动目标
- `components/lyrics/lyrics-window.tsx` — flow layout 容器样式可能需调整
- `app/globals.css` — 如需新增居中相关 CSS

# Current State
- 任务：古诗词音频 AI 情感生成
- 已完成：
  - `scripts/generate-poetry-audio.ts` 两阶段管线脚本（DeepSeek 分析 + StepFun TTS）
  - DeepSeek 分析诗歌内容 → 智能选择音色(12种) + 生成全局 instruction + 逐行情感标注
  - StepFun stepaudio-2.5-tts + Step Plan 配额，支持括号内联情感控制
  - 括号注解清理（`（一作：…）`），恢复机制（跳过已有音频），增量分析缓存
  - 测试验证：sc200 前 6 首生成成功，音色选择精准
- 产出物：
  - `scripts/generate-poetry-audio.ts` — 主脚本
  - `data/tts-analysis.json` — 分析缓存（6 条）
  - `public/audio/poetry/` — 6 个新 MP3 文件
- 作者简介补全也已完成：234/333 bio, 66/250 lifeStory, 250/333 头像

# Next Action
- 全量执行音频生成（538 首缺音频，需分多次运行控制 Step Plan 配额）：
  ```bash
  # 按 sc200 → gs300 → ts300 顺序
  npx tsx scripts/generate-poetry-audio.ts --write --source sc200
  npx tsx scripts/generate-poetry-audio.ts --write --source gs300
  npx tsx scripts/generate-poetry-audio.ts --write --source ts300
  ```
- 每次中断后重新运行，恢复机制自动跳过已生成

# Blockers
- Step Plan 周配额限制：538 首可能需要跨周完成
- 无其他阻塞

# Key Decisions
- DeepSeek 分析结果缓存到 `data/tts-analysis.json`（不存 DB），脚本无 DB 依赖
- 使用 Step Plan 端点 (`/step_plan/v1/audio/speech`) 而非标准端点
- validation 放宽：允许 DeepSeek 自由拆分行，70% 原文字符重叠即通过

# Relevant Files
- `scripts/generate-poetry-audio.ts` — 音频生成脚本
- `data/tts-analysis.json` — 分析缓存
- `scripts/scrape-authors.ts` — 作者简介补全脚本（已完成）
- `data/authors.json` — 333 位作者数据（已补全）

# Current State
- 任务：拼音能力实现（目前仅有按钮 UI，未接入数据源和渲染逻辑）
- 已完成：首页布局重构 — 2×2 网格、去框化排版、打卡条、水墨线条图标
- 产出物：
  - app/page.tsx — 首页 2×2 功能入口 + 头部"我的"链接
  - components/home/today-poetry-hero.tsx — 去框化诗词排版 + 打卡条
  - components/home/weekly-streak-matrix.tsx — 7 日打卡圆点矩阵
  - lib/stats/weekly-checkin.ts — 周打卡数据函数（DI 模式）
  - tests/stats/weekly-checkin.test.ts — 7 个单测
  - components/poster/poetry-poster.tsx — 新增 badgeClassName prop

# Next Action（原子级）
- 确定拼音数据来源：检查 data/ts300.simple.json 和 data/ts300.raw.json 中是否已有拼音字段，或需要引入外部拼音库（如 pinyin-pro），然后实现 LyricsWindow 的 showPinyin=true 完整路径

# Blockers
- None

# Key Decisions（≤ 3 条）
- 首页主容器去掉 border，改用 shadow-[0_8px_30px_rgba(0,0,0,0.04)] 弥散阴影分层
- CTA 按钮后置于诗词正文下方，阅读动线：标题→作者→打卡→读诗→行动
- PoetryPoster 新增 badgeClassName prop（向后兼容，其他调用点使用默认 left-4 top-4）

# Relevant Files（拼音功能最小集）
- components/lyrics/lyrics-window.tsx — 已有 renderRubyText() 和 showPinyin 逻辑骨架
- data/ts300.simple.json — 检查是否有 pinyin 字段
- data/ts300.raw.json — 同上
- components/review/review-player.tsx — 复习页使用 LyricsWindow，showPinyin 交互入口
- components/poetry/poetry-detail-client.tsx — 详情页可能的拼音切换入口

# Current State
- 项目：唐诗画境 (tang-poetry)
- 已完成：全局设计规范重构 + 诗词详情页优化 + 诗人页优化 + 作者头像入口
- 验证：`npm run build` ✅ + `npm test` 177 pass ✅
- 未提交：所有设计变更尚未 commit

# 已完成工作摘要

## 1. 全局色彩系统（globals.css @theme inline）
| Token | 色值 | 用途 |
|---|---|---|
| paper | #F7F5F0 | 宣纸白，页面底色 |
| surface | #FFFFFF | 卡片/面板底色 |
| primary | #354E6B | 黛蓝，品牌色/按钮 |
| accent | #B84B4B | 朱砂红，错题/未掌握 |
| ink-900 | #2C2C2C | 深墨，主文本 |
| ink-600 | #666666 | 灰墨，次文本 |
| ink-400 | #999999 | 浅灰，弱文本/标签 |
| ink-200 | #D4D4D4 | 边框/分割线 |

## 2. 字体排版
- Noto Serif SC CDN（400/700），@theme（非 inline）定义 --font-serif
- h1-h6 自动 font-serif，UI 控件 font-sans

## 3. 复习页重构
- 看板去卡片化 → flex 竖线分割
- 歌词 flow 布局（去气泡）+ HTML5 ruby/rt 拼音注音
- Sticky footer（backdrop-blur）含播放控制 + 自评按钮

## 4. 诗词详情页优化
- 补全诗歌标题（大号衬线 h1）
- 作者入口：头像图片 / 首字兜底，链接到 /author/[name]
- 轻量化图标播放器 + 细进度条
- 译文+AI讲解整合面板（embedded 模式）
- PosterStatusBadge 可读性增强

## 5. 诗人页优化
- 简介区左对齐 + max-width 65ch + 按句号分段
- 作品列表多列网格 + 标签聚拢 + 箭头 hover
- 面包屑导航 + "返回"按钮（referer 回退）
- 头像水墨晕染效果

# Next Action
- **首页布局重构**（/ 路由，app/page.tsx + components/home/）

# Key Decisions
- 色彩 token 通过 @theme inline 注入，组件同时支持 var() 和 Tailwind utility
- 字体用非 inline @theme 确保 CSS 变量生成（CDN 字体覆盖）
- PoetryDetail 新增 authorAvatarUrl 字段（从 authors.json 查找）
- 诗人页返回按钮用 referer，非 JS router.back()

# Relevant Files — 首页相关
- app/page.tsx — 首页路由（Server Component）
- components/home/today-poetry-hero.tsx — 今日诗歌主组件
- app/globals.css — 色彩/字体 token 定义
- app/layout.tsx — 全局布局 + 字体 CDN

# Current State

- 任务：继续把简繁切换从首页/详情页扩展到复习页与挑战页，统一所有诗歌内容读取都走 `cookie` 驱动的内容变体层
- 已完成：`lib/poetry/daily.ts` 与 `lib/poetry/repository.ts` 已按 `cookie` 返回简繁题目/作者/正文；详情页已接入 `ScriptVariantToggle`；首页“查看今日一诗”已修正为锚点跳转；详情页音频状态已改为按静态文件存在判断；开发期 Prisma 变更兼容分支已移除并确认通过重启 dev server 生效
- 产出物：`app/page.tsx`、`app/poetry/[id]/page.tsx`、`components/poetry/script-variant-toggle.tsx`、`components/poetry/poetry-detail.tsx`、`lib/poetry/script-variant.ts`、`lib/poetry/daily.ts`、`lib/poetry/repository.ts`、`lib/audio.ts`、`AGENTS.md`

# Next Action

- 修改 `lib/review/scheduler.ts` 的 `ReviewStateRecord.poetry` 查询与 `toSnapshot()`，让复习列表和复习播放页按 `ScriptVariant` 返回简繁题目、作者、预览诗句

# Blockers

- None

# Key Decisions

- 开发阶段 Prisma schema 变更不在业务代码里做旧 runtime client 兼容：改完后直接重启服务
- 简繁切换只作用于诗题、作者、正文：固定 UI 文案不切换
- SSR 以 `cookie` 为真相源，`localStorage` 只做客户端镜像

# Relevant Files

- `lib/poetry/script-variant.ts`
- `lib/review/scheduler.ts`
- `components/review/review-list.tsx`
- `app/review/page.tsx`
- `app/review/[id]/page.tsx`
- `lib/poetry/daily.ts`
- `lib/poetry/repository.ts`
- `AGENTS.md`

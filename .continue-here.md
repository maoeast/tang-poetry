# Current State
- 任务：/browse 搜索功能已完成；下一步待定
- 已完成（本轮）：
  - `lib/browse/repository.ts` 新增 `searchPoems(query, scriptVariant)` — 全量取诗 + TS 层按标题/作者/诗句（简繁均覆盖）过滤
  - `components/browse/search-input.tsx` — 客户端组件，300ms 防抖 URL `?q=` 更新，清除按钮，Suspense 包裹
  - `app/browse/page.tsx` — 读取 `searchParams.q`，搜索模式显示扁平结果网格（隐藏分类导航），空结果提示
  - `tests/browse/repository.test.ts` 新增 7 个 searchPoems 测试（标题/作者/诗句/繁体/空结果/大小写/变体解析）
- 产出物：
  - `lib/browse/repository.ts` — searchPoems()
  - `components/browse/search-input.tsx` — SearchInput client component
  - `app/browse/page.tsx` — search mode support
  - `tests/browse/repository.test.ts` — 7 new tests

# Next Action
- 待定 — 可以继续 Task 13 集成测试或其他功能需求

# Blockers
- None

# Key Decisions
- 搜索采用 URL query params (`?q=xxx`) + Server Component 重新获取数据模式，无需额外 API route
- 366 首诗全量取回 + TS 层过滤，兼顾简繁体搜索覆盖
- 搜索模式隐藏分类吸顶导航，展示扁平结果网格 + 空状态提示
- SearchInput 使用 Suspense 包裹以支持 useSearchParams，通过 key 机制同步浏览器前进/后退

# Relevant Files
- `app/browse/page.tsx`
- `components/browse/search-input.tsx`
- `components/browse/sticky-category-nav.tsx`
- `components/browse/category-section.tsx`
- `components/browse/poetry-card.tsx`
- `lib/browse/repository.ts`
- `tests/browse/repository.test.ts`

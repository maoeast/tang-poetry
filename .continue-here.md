# Current State
- 任务：继续补齐 `G:\tang poetry` 中唐诗条目的译文/注释，并完成 `ts300-0145`《山中送别》的独立音频生成与切换
- 已完成：新增 `Poetry.annotation` 字段与迁移；实现古文岛批量导入脚本；实现定点补录脚本；修正 `ts300-0145` 标题为《山中送别》；临时将 `ts300-0145` 音频映射到 `ts300-0054` 现有音频；补录/精修一批译文与注释
- 产出物：`prisma/migrations/202606040001_add_poetry_annotation/`、`scripts/import-guwendao-annotations.ts`、`scripts/fill-remaining-guwendao.ts`、`data/manual-poetry-supplements.json`

# Next Action
- 修改 `scripts/generate-tts-audio.py` 的请求参数或生成路径，使 `--poem-id c8a4faa6-8666-44f9-b4c9-df78d7af844d` 能成功生成 `public/audio/poetry/c8a4faa6-8666-44f9-b4c9-df78d7af844d.mp3`，随后删除 `lib/audio.ts` 中 `ts300-0145` 的临时音频复用映射

# Blockers
- Stepfun TTS 真实调用当前返回 `400 no available voice synthensis text`，`STEPFUN_API_KEY` 已确认来自 `.env.local`

# Key Decisions
- 手工修订源统一维护在 `data/manual-poetry-supplements.json`：避免后续重复回放脚本覆盖人工修正
- 当前库缺失但古文岛存在的诗，允许以 `ts300-extra-*` 新建入库：先补可用数据，再做后续归并
- `ts300-0145` 先复用 `ts300-0054` 音频：保证页面先可播，待独立 TTS 成功后再切回自身 `sourceUid`

# Relevant Files
- G:\tang poetry\lib\audio.ts
- G:\tang poetry\scripts\generate-tts-audio.py
- G:\tang poetry\tests\audio\audio.test.ts
- G:\tang poetry\data\manual-poetry-supplements.json
- G:\tang poetry\scripts\fill-remaining-guwendao.ts

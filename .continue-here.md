# Current State

- 任务：确认当前诗歌页面音频文件命名规则，并为下一会话的 mp3 批量接入准备最小交接
- 已完成：已修复复习播放页 hydration mismatch；已把诗歌详情页 AI 讲解成功链路纳入 Playwright smoke；已确认页面当前通过 `getAudioUrl(poetryId)` 按 `/audio/poetry/${poetryId}.mp3` 取音频
- 产出物：`components/review/review-poetry-stage.tsx`、`lib/review/player-queue.ts`、`tests/review/player-queue.test.ts`、`tests/e2e/smoke.spec.ts`、`next.config.js`

# Next Action

- 对照 `data/ts300.simple.json` 与 `public/audio/poetry/*.mp3`，建立现有 UUID 文件名到项目 `poetryId` 的映射方案，并决定是重命名文件还是扩展音频元数据字段

# Blockers

- 当前页面音频 URL 规则是 `${poetryId}.mp3`，但 `public/audio/poetry` 里的 366 个文件实际是 UUID 文件名，尚未与 `ts300-*` 诗歌 ID 建立映射

# Key Decisions

- AI 密钥只放本地 `.env.local`：不写入仓库跟踪文件
- 首页已读 CTA 必须保持跳转 `/challenge?poetryId=...`：这是已验证行为，后续改音频时不能破坏
- 当前代码取音频固定走 `getAudioUrl(poetryId)`：默认路径 `/audio/poetry/${poetryId}.mp3`

# Relevant Files

- `lib/audio.ts`
- `public/audio/poetry/`
- `data/ts300.simple.json`
- `lib/poetry/repository.ts`
- `components/poetry/immersive-poetry-stage.tsx`
- `components/review/review-poetry-stage.tsx`
